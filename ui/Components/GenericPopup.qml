import QtQuick
import QtQuick.Controls

import Ui.Theme

Popup {
    width: 600
    height: 300
    modal: false
    focus: false

    background: Rectangle {
        color: Colors.grey700
        radius: 4
    }
}
